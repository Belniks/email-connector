import { Recipient } from '../interfaces/recipient.interface';
export declare const RecipientMapper: {
    fromGraph: (recipient: any) => Recipient;
    fromGraphArray: (recipients: any[]) => Recipient[];
};
